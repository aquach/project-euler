object Problem124 {

  def rad(x: Int) = {
  }

  def main(args: Array[String]) {
    println((3 to 1000).map({ a =>
      (0 to 100).foreach({ n => println(a, n, r(a, n)) }) 
      12
    }).sum)
  }
}
